#include <stdio.h>
#include <stdlib.h>

    int main (void)
        {

        int cont;
        //Contagem Regressiva
        for(cont = 10; cont > 0; cont--) {
            printf("\n%d", cont );
        }
        return 0;
        }
